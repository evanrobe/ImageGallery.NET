
CREATE TABLE [IMAGE] (
  I_ID INT NOT NULL IDENTITY,
  I_GUID nvarchar(36) NOT NULL,
  I_FILE_NAME NVARCHAR(2000) NOT NULL,
  I_CONTENT_TYPE NVARCHAR(2000) NOT NULL
);
GO

ALTER TABLE [IMAGE] ADD CONSTRAINT [PK_IMAGE] PRIMARY KEY ([I_ID]);
GO

alter
table
image
add constraint IMAGE_UC1 UNIQUE(I_GUID)
GO

CREATE TABLE [IMAGE_KEYWORDS] (
  [IK_ID] int NOT NULL identity,
  I_ID int not null
);
GO

ALTER TABLE [IMAGE_KEYWORDS] ADD CONSTRAINT [PK_IMAGE_KEYWORDS] PRIMARY KEY ([IK_ID]);
GO

ALTER TABLE
IMAGE_KEYWORDS
ADD CONSTRAINT IK_I_FK1 FOREIGN KEY
(I_ID) REFERENCES IMAGE(I_ID)
GO

CREATE TABLE
IMAGE_TAG
(
IT_ID INT NOT NULL IDENTITY,
I_ID INT NOT NULL,
IT_NAME NVARCHAR(2000)
);
GO

ALTER
TABLE
IMAGE_TAG
ADD CONSTRAINT PK_IMAGE_TAG PRIMARY KEY (IT_ID);
GO



